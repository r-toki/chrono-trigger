type ChronoConfig = {
  startTime: Date;
  endTime: Date;
  secondsToGenerate: number;
};

export const chrono = (currentTime: Date, { startTime, endTime, secondsToGenerate }: ChronoConfig) => {
  return;
};
